apiVersion: v1
kind: Secret
metadata:
  name: external-api-secrets
  namespace: samsung-recipe
type: Opaque
data:
  # Base64 encoded values - replace with actual encoded keys
  unsplash-access-key: ""  # echo -n "your_unsplash_key" | base64
  google-translate-api-key: ""  # echo -n "your_google_translate_key" | base64
---
apiVersion: v1
kind: Secret
metadata:
  name: database-secrets
  namespace: samsung-recipe
type: Opaque
data:
  # Base64 encoded database credentials (GCP Cloud SQL)
  # echo -n "p646626910485-oprfkm@gcp-sa-cloud-sql.iam.gserviceaccount.com" | base64
  postgres-user: ""  # 실제 GCP 서비스 계정 이메일을 base64로 인코딩
  # echo -n "실제_GCP_SQL_비밀번호" | base64
  postgres-password: ""  # 실제 GCP Cloud SQL 비밀번호를 base64로 인코딩
  postgres-db: "cmVjaXBlX2Ri"  # recipe_db